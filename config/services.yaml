# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
    # Taguer le contrôleur avec 'controller.service_arguments' pour permettre l'injection automatique des dépendances
    App\Controller\TranslateController:
        arguments:
            $codeTranslator: '@App\Service\CodeTranslator'
        tags: ['controller.service_arguments']
    App\Controller\GenericCategoryController:
        tags: ['controller.service_arguments']
    # Déclaration explicite du service CodeTranslator
    App\Service\CodeTranslator:
        arguments:
            $entityManager: '@doctrine.orm.entity_manager'

    
    App\DataProvider\GenericTranslationDataProvider:
        decorates: 'api_platform.doctrine.orm.state.item_provider'
        arguments:
            $decorated: '@App\DataProvider\GenericTranslationDataProvider.inner'
            $codeTranslator: '@App\Service\CodeTranslator'
        autowire: true
        autoconfigure: false

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
